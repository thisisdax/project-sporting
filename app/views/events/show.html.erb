<div class="jumbotron">
  <div class="container-fluid text-center">
    <div class="row content">
      <div class="col-sm-2 sidenav">
        <p><a href="#">Link</a></p>
        <p><a href="#">Link</a></p>
        <p><a href="#">Link</a></p>
      </div>
      <div class="col-sm-8 text-left">
        <h1><%= @event.name %></h1>
        <h2><%= @event.type_of_sport %></h2>
        <li>Date: <%= @event.date.to_formatted_s(:long) %></li>
        <li>Time: <%= @event.time.to_formatted_s(:time) %></li>
        <li>Location: <%= @event.location %></li>
        <li>Min <%= @event.minpax %> - Max <%= @event.maxpax %> Pax</li>
        <hr>
        <div class="row">
        <div class="col-md-6">
        <h3>Description</h3>
        <p><%= @event.description %></p>
        </div>
        <div class="col-md-6">
        <%= link_to "Join", events_path, { :class=>"btn btn-success" } %>
        </div>
        </div>
      </div>
      <div class="col-sm-2 sidenav">
        <div class="well">
          <p>ADS</p>
        </div>
        <div class="well">
          <p>ADS</p>
        </div>
      </div>
    </div>
  </div>
  things to add :
  people in the event(left bar)/friends(right bar)
  google(beside the div class showing all details, store both div into a row)

  <h3>This is supposed to be the comments section:</h3>
  <%= render 'comments/comment' %>
  <footer class="container-fluid text-center">
    <p>Footer Text</p>
  </footer>

</div>
